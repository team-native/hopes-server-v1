spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: hopes-server
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration/{vendor}
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.starttls.required]": true
      "[mail.smtp.connectiontimeout]": 5000
      "[mail.smtp.timeout]": 5000
      "[mail.smtp.writetimeout]": 5000

server:
  port: ${PORT}
  forward-headers-strategy: ${SERVER_FORWARD_HEADERS_STRATEGY:none}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    path: /v3/api-docs

hopes:
  ai:
    enabled: ${AI_ENABLED:true}
    gemini-api-key: ${GEMINI_API_KEY:}
    chat-model: ${AI_CHAT_MODEL:gemini-3.1-flash-lite}
    # 임베딩은 로컬(DJL + PyTorch). embedding-model은 캐시 태그(모델 바뀌면 재임베딩), url은 DJL 모델 위치.
    embedding-model: ${AI_EMBEDDING_MODEL:intfloat/multilingual-e5-large}
    embedding-model-url: ${AI_EMBEDDING_MODEL_URL:djl://ai.djl.huggingface.pytorch/intfloat/multilingual-e5-large}
    embedding-batch-size: ${AI_EMBEDDING_BATCH_SIZE:4}
    chunks-path: ${AI_CHUNKS_PATH:classpath:data/gsm_guide_rag_chunks.jsonl}
    cache-path: ${AI_CACHE_PATH:./data/embeddings_cache.json}
    top-k: ${AI_TOP_K:3}
    min-similarity: ${AI_MIN_SIMILARITY:0.55}
    history-max-turns: ${AI_HISTORY_MAX_TURNS:20}
    max-output-tokens: ${AI_MAX_OUTPUT_TOKENS:640}
    index-retry-seconds: ${AI_INDEX_RETRY_SECONDS:30}
  rate-limit:
    messages-per-minute: ${RATE_LIMIT_MESSAGES_PER_MINUTE:10}
    login-attempts-per-minute: ${RATE_LIMIT_LOGIN_ATTEMPTS_PER_MINUTE:5}
    verification-requests-per-minute: ${RATE_LIMIT_VERIFICATION_REQUESTS_PER_MINUTE:3}
    verification-attempts-per-minute: ${RATE_LIMIT_VERIFICATION_ATTEMPTS_PER_MINUTE:5}
    auth-requests-per-ip-per-minute: ${RATE_LIMIT_AUTH_REQUESTS_PER_IP_PER_MINUTE:60}
  auth:
    token-secret: ${TOKEN_SECRET}
    token-validity-hours: ${TOKEN_VALIDITY_HOURS}
  verification:
    ttl-minutes: ${VERIFICATION_TTL_MINUTES}
  mail:
    enabled: ${MAIL_ENABLED}
    from: ${MAIL_FROM}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  discord:
    question-log-webhook-url: ${DISCORD_QUESTION_LOG_WEBHOOK_URL:}
